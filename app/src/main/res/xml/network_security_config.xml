<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Prefer switching the API to HTTPS. This is a compatibility fallback
      for the current HTTP base URL used by RetrofitClient.
    -->
    <!-- Allow cleartext globally for dev environments -->
    <base-config cleartextTrafficPermitted="true" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- IP-based domain entry for http://172.188.97.210:8000 -->
        <domain includeSubdomains="false">172.188.97.210</domain>
    </domain-config>
</network-security-config>
